{{ $headless := false }}
{{ $hostname := .chezmoi.hostname }}
{{ $transient := false }}
{{ $node := true }}
{{ $python := true }}
{{ $ruby := false }}
{{ $work := false }}
{{ $editor := "code" }}

{{- if eq .chezmoi.hostname "x86" -}}
  {{ $work = true }}
{{- end -}}

[data]
  headless = {{ $headless }}
  hostname = {{ $hostname | quote }}
  transient = {{ $transient }}
  work = {{ $work }}
  python = {{ $python }}
  ruby = {{ $ruby }}
  node = {{ $node }}
  editor = {{ $editor | quote }}